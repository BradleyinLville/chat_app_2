// import 'package:flutter/services.dart';

// class ProfileHelper {
//   ProfileHelper();

//   Future<String> loadProfileTestingAsset() async {
//     return await rootBundle.loadString('assets/testing_list.json');}

//lots of parsed ProfileModels
// static List<ProfileModel> parseProfile(String response) {
//   final parsed = json.decode(response).cast<Map<String, dynamic>>();
//   return parsed
//       .map<ProfileModel>((json) => ProfileModel.fromJson(json))
//       .toList();
// }

//for just one profile  may not use
// static List<ProfileModel> parseMyProfile(String response) {
//   final parsed = json.decode(response).cast<Map<String, dynamic>>();
//   return parsed
//       .map<ProfileModel>((json) => ProfileModel.fromJson(json))
//       .toList();
// }
// }
